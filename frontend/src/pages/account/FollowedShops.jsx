import React,{useEffect,useState}from"react";import{Link}from"react-router-dom";import{Store}from"lucide-react";import{api}from"@/lib/api";import{EmptyState,Loader}from"@/components/shared/Bits";import ShopCard from"@/components/marketplace/ShopCard";
export default function FollowedShops(){const[shops,setShops]=useState(null);useEffect(()=>{api.get("/account/followed-shops").then(({data})=>setShops(data)).catch(()=>setShops([]));},[]);if(!shops)return <Loader/>;return <div><h1 className="text-2xl font-bold">Shops You Follow</h1><p className="mt-1 text-sm text-nexora-muted">New finds from shops you chose to follow.</p>{shops.length?<div className="mt-5 grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{shops.map(s=><ShopCard key={s.id} shop={s}/>)}</div>:<div className="mt-6"><EmptyState icon={Store} title="You aren’t following any shops yet" description="Follow a shop to find it here and see its products on your homepage." action={<Link to="/shops" className="nx-btn-primary mt-2">Browse shops</Link>}/></div>}</div>}
